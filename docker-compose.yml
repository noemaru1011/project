services:
  db:
    image: postgres:16
    container_name: db
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: always
    env_file:
      - .env
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  backend:
    build:
      context: .
      dockerfile: backend/dockerfile
    container_name: backend
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
      - backend/.env
    environment:
      REDIS_URL: redis://redis:6379
    depends_on:
      - db
      - redis
    volumes:
      - ./shared:/project/shared
      - ./backend/prisma:/project/backend/prisma
      - ./backend/src:/project/backend/src

  redis:
    image: redis:7
    ports:
      - "6379:6379"
  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "5173:5173"
  #   volumes:
  #     - ./shared:/project/shared

volumes:
  db_data:
